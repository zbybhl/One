<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/item_one"
        android:icon="@mipmap/ic_radio"
        android:title="ONE"
        android:checked="true"/>
    <item
        android:id="@+id/item_all"
        android:icon="@mipmap/ic_pages"
        android:title="ALL" />
    <item
        android:id="@+id/item_me"
        android:icon="@mipmap/ic_person"
        android:title="ME" />

</menu>
